
cd .\twittos\

npm install express mysql2 cors 

        //SQL//

cat ./db-lib/init.sql | docker exec -i twittos-db-1 mysql -u root -proot  (lancer init.sql)
                        ou
cat ./db-lib/del.sql | docker exec -i twittos-db-1 mysql -u root -proot (lancer del.sql)

        //DockerComose//

docker compose up -d
docker compose exec node npm install
docker compose exec node npm run dev -- --host --port 5000
            (http://localhost:8081)
            (http://localhost:8000)
            
        //API//

npm run server 
            (http://localhost:3000/tweet)
            (http://localhost:3000/mp)
            (http://localhost:3000/profil)

        //Docker//

docker ps /   docker ps -a  / docker images / docker network  --> Commandes pour afficher des datas

# Procedure pour lancer :
docker build -t twittos .
Docker run -p 8000:80 -it --name twittos twittos